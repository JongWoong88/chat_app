spring:
  application:
    name: chat_app
  datasource:
    url: jdbc:log4jdbc:mysql://localhost:3306/woong
    driver-class-name: net.sf.log4jdbc.sql.jdbcapi.DriverSpy
    username: root
    password: whddnd88#$
  sql:
    init:
      mode: always
  devtools:
    livereload:
      enabled: true
  thymeleaf:
    cache: false

mybatis:
  mapper-locations: classpath:mybatis/mapper/*.xml
  config-location: classpath:mybatis/mybatisConfig.xml

logging:
  config: classpath:log4j.xml
  level:
    jdbc:
      sqlonly: info
      sqltiming: off
      resultsettable: info
      audit: off
      resultset: off
      connection: off

